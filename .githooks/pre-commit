#!/bin/sh
# pre-commit hook — 跑 skill-aware lint
#
# 自動啟用: package.json 的 "prepare" script 會 git config core.hooksPath .githooks
# 手動啟用: git config core.hooksPath .githooks
# 跳過一次: git commit --no-verify
#
# Hook 內只跑 staged files (快), 想 lint 全專案: npm run skill-lint:all
node scripts/skill-lint.mjs
